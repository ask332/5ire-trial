/// <reference types="bn.js" />
import type { Vec } from '@5ire/types';
import type { EventRecord } from '@5ire/types/interfaces';
import type { BN } from '@5ire/util';
interface Changes {
    added: string[];
    blockHash: string;
    removed: string[];
}
export declare function extractContributed(paraId: string | number | BN, events: Vec<EventRecord>): Changes;
export {};
