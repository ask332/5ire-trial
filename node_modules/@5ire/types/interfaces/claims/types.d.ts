import type { Enum } from '@5ire/types';
/** @name StatementKind */
export interface StatementKind extends Enum {
    readonly isRegular: boolean;
    readonly isSaft: boolean;
}
export declare type PHANTOM_CLAIMS = 'claims';
